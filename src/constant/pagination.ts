// 默认分页数量
export const PAGE_SIZE = 10

// 分页规格
export const PAGE_SIZES = [10, 20, 30, 40]

// 分页组件layout布局
export const PAGE_LAYOUT = 'total, sizes, prev, pager, next, jumper'

// 分页组件背景色
export const PAGE_BACKGROUND = true

// 分页组件默认属性配置
export const PAGE_DEFAULT_ATTRS = {
  layout: PAGE_LAYOUT,
  background: PAGE_BACKGROUND,
  'page-sizes': PAGE_SIZES,
}
