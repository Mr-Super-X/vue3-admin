export interface IState {
  test?: string
}

const state: IState = {
  test: 'test'
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
